---
// @ts-nocheck
import Card from "../../ui/Card.astro";
import NivelBadge from "../../ui/NivelBadge.astro";
import { calculateLevel } from "../../../config/levelsConfig.js";
const { puntosActuales = 1500 } = Astro.props;

// Nivel inicial server-side para que el badge no "salte" visualmente al hidratar.
const nivelInicial = calculateLevel(puntosActuales) || { nombre: "Novato (Cachorro)", icono: "🌱" };
---

<div class="modulo-niveles-container w-full grid grid-cols-1 xl:grid-cols-12 gap-6 items-start">
  <!-- Tarjeta de Perfil -->
  <Card
    id="perfil-card"
    class="perfil-card xl:col-span-12 border-2 border-[#60A5FA] relative overflow-hidden transition-all duration-500"
    style="box-shadow: 0 0 20px rgba(96, 165, 250, 0.15);"
  >
    <!-- Glow decorativo del nivel -->
    <div
      id="glow-nivel"
      class="absolute top-0 right-0 w-40 h-40 -full blur-[60px] pointer-events-none"
      style="background: rgba(96, 165, 250, 0.2);"
    >
    </div>
    <!-- Shine effect diagonal en toda la tarjeta -->
    <div
      class="shine-effect absolute inset-0 -3xl pointer-events-none z-30"
    >
    </div>

    <div class="relative z-10">
      <div class="flex items-center justify-between mb-4">
        <div>
          <h2 class="text-black text-xl font-bold">
            Hola, <span id="user-greeting">Estudiante</span>
          </h2>
          <p class="text-gray-700 text-sm font-semibold">Tu progreso académico</p>
        </div>
        <NivelBadge
          id="nivel-badge"
          nombre={nivelInicial.nombre}
          xp={puntosActuales}
          icono={nivelInicial.icono}
          showDetails={true}
          extraClass="bg-[#60A5FA]/10 border-2 border-[#60A5FA] px-6 py-3 -xl text-[#60A5FA] font-bold text-lg shadow-[0_0_25px_rgba(96,165,250,0.3)] transition-all duration-300 hover:scale-105"
        />
      </div>

      <!-- Barra de Progreso -->
      <div class="mb-4">
        <div class="flex justify-between text-xs text-slate-400 mb-2">
          <span id="xp-actual" class="text-black font-bold">1,500 XP</span>
          <div class="flex items-center gap-2">
            <span id="xp-siguiente" class="text-black font-bold">2,000 XP</span>
            <span id="xp-porcentaje" class="text-black font-bold">75%</span>
          </div>
        </div>
        <div class="h-3 bg-[#e5e5e5] border-2 border-black -full overflow-hidden relative">
          <div
            id="progreso-bar"
            class="h-full -full transition-all duration-500 ease-out relative"
            style="width: 75%; background: linear-gradient(90deg, #60A5FA, #3B82F6);"
          >
            <div
              class="absolute inset-0 bg-linear-to-r from-transparent via-white/30 to-transparent w-full animate-shimmer"
            >
            </div>
          </div>
        </div>
        <p id="xp-faltante" class="text-black font-semibold text-xs mt-2 text-center">
          Faltan 500 XP para el siguiente nivel
        </p>
      </div>

      <!-- Info de XP ganada hoy -->
      <div
        class="flex items-center justify-between bg-white border-2 border-black -xl py-2 px-4"
      >
        <div class="flex items-center gap-2">
          <span
            id="xp-indicator"
            class="w-3 h-3 -full bg-brand-lime animate-pulse shadow-[0_0_8px_rgba(215,253,73,0.6)]"
          ></span>
          <span id="xp-hoy" class="text-black text-sm font-semibold"
            >+0 XP obtenida hoy</span
          >
        </div>
      </div>
    </div>
  </Card>

  <!-- Línea de Tiempo de Niveles -->
  <Card
    id="timeline-section"
    class="timeline-section xl:col-span-7 bg-black/60 backdrop-blur-lg border-2 border-black p-6 transition-all duration-500 xl:min-h-155"
  >
    <h3 class="text-white text-lg font-bold mb-6 flex items-center gap-2">
      <svg
        class="w-5 h-5 text-brand-lime"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"
        ></path>
      </svg>
      Tu Carrera
    </h3>

    <div id="timeline-container" class="space-y-4">
      <!-- Los niveles se generan dinámicamente con JavaScript -->
    </div>
  </Card>

  <!-- Beneficios por Nivel -->
  <Card
    id="beneficios-section"
    class="beneficios-section xl:col-span-5 bg-black/60 backdrop-blur-lg border-2 border-black p-6 transition-all duration-500 h-full xl:min-h-155"
  >
    <h3 class="text-white text-lg font-bold mb-6 flex items-center gap-2">
      <svg
        class="w-5 h-5 text-brand-lime"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M12 8v13m0-13V6a2 2 0 112 2h-2zm0 0V5.5A2.5 2.5 0 109.5 8H12zm-7 4h14M5 12a2 2 0 110-4h14a2 2 0 110 4M5 12v7a2 2 0 002 2h10a2 2 0 002-2v-7"
        ></path>
      </svg>
      Beneficios por Nivel
    </h3>

    <div id="beneficios-grid" class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-1 gap-4">
      <!-- Los beneficios se generan dinámicamente con JavaScript -->
    </div>
  </Card>

</div>

<style>
  html {
    scroll-behavior: smooth;
  }

  @keyframes shimmer {
    0% {
      transform: translateX(-100%);
    }
    100% {
      transform: translateX(100%);
    }
  }

  .animate-shimmer {
    animation: shimmer 2s infinite;
  }

  /* Nivel Activo - Glow Effect */
  .nivel-activo {
    position: relative;
  }

  .nivel-activo::before {
    content: "";
    position: absolute;
    inset: -2px;
    border-radius: 12px;
    background: linear-gradient(
      135deg,
      var(--nivel-color),
      var(--nivel-color-glow)
    );
    filter: blur(8px);
    opacity: 0.4;
    z-index: -1;
  }

  /* Nivel Bloqueado */
  .nivel-bloqueado {
    opacity: 0.4;
    filter: grayscale(100%);
  }

  /* Tarjeta de Beneficio */
  .beneficio-card {
    transition: all 0.3s ease;
  }

  .beneficio-card:hover:not(.bloqueado) {
    transform: translateY(-2px);
    border-color: var(--beneficio-color);
  }

  /* Shine Effect Diagonal tipo slash */
  .shine-effect::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      115deg,
      transparent 0%,
      transparent 25%,
      rgba(255, 255, 255, 0.5) 40%,
      rgba(255, 255, 255, 0.8) 50%,
      rgba(255, 255, 255, 0.5) 60%,
      transparent 75%,
      transparent 100%
    );
    transform: translateX(-100%);
    animation: shineSlash 5s infinite;
  }

  @keyframes shineSlash {
    0% {
      transform: translateX(-100%);
    }
    40% {
      transform: translateX(100%);
    }
    100% {
      transform: translateX(100%);
    }
  }

  /* Respetar preferencias de accesibilidad para reducir animaciones */
  @media (prefers-reduced-motion: reduce) {
    .animate-shimmer {
      animation: none !important;
    }
    .shine-effect::before {
      animation: none !important;
    }
    .nivel-activo::before {
      transition: none !important;
      filter: none !important;
    }
    * {
      transition-duration: 0s !important;
    }
  }
</style>

<script type="module" define:vars={{ puntosActuales }}>
  // Configuración centralizada de niveles
  const levels = [
    {
      id: 1,
      nombre: 'Novato (Cachorro)',
      puntosMin: 0,
      puntosMax: 500,
      color: '#9CA3AF',
      colorGlow: '#6B7280',
      icono: '🌱',
      rango: '0 - 500 XP',
      beneficios: []
    },
    {
      id: 2,
      nombre: 'Bachiller',
      puntosMin: 501,
      puntosMax: 2000,
      color: '#60A5FA',
      colorGlow: '#3B82F6',
      icono: '🎓',
      rango: '501 - 2,000 XP',
      beneficios: ['5% descuento en comedor']
    },
    {
      id: 3,
      nombre: 'Licenciado',
      puntosMin: 2001,
      puntosMax: 5000,
      color: '#A78BFA',
      colorGlow: '#8B5CF6',
      icono: '📚',
      rango: '2,001 - 5,000 XP',
      beneficios: ['10% descuento en comedor']
    },
    {
      id: 4,
      nombre: 'Magíster',
      puntosMin: 5001,
      puntosMax: 10000,
      color: '#34D399',
      colorGlow: '#10B981',
      icono: '🎖️',
      rango: '5,001 - 10,000 XP',
      beneficios: ['15% descuento en comedor', 'Acceso VIP']
    },
    {
      id: 5,
      nombre: 'Doctor',
      puntosMin: 10001,
      puntosMax: 20000,
      color: '#F59E0B',
      colorGlow: '#D97706',
      icono: '👑',
      rango: '10,001 - 20,000 XP',
      beneficios: ['20% descuento en comedor', 'Acceso VIP']
    },
    {
      id: 6,
      nombre: 'Capy Legend',
      puntosMin: 20001,
      puntosMax: Infinity,
      color: '#F472B6',
      colorGlow: '#EC4899',
      icono: '⭐',
      rango: '20,001+ XP',
      beneficios: ['25% descuento en comedor', 'Acceso VIP']
    }
  ];

  // Función para calcular nivel basado en XP
  function calculateLevel(xp) {
    if (xp < 0) return null; // Validación básica

    for (let i = levels.length - 1; i >= 0; i--) {
      const level = levels[i];
      if (xp >= level.puntosMin) {
        const progress = Math.min(100, ((xp - level.puntosMin) / (level.puntosMax - level.puntosMin)) * 100);
        const nextXp = level.puntosMax === Infinity ? null : level.puntosMax - xp;
        return {
          ...level,
          progress: Math.round(progress),
          nextXp: nextXp > 0 ? nextXp : 0,
          isMaxLevel: level.puntosMax === Infinity
        };
      }
    }
    return null; // XP insuficiente para cualquier nivel
  }

  let currentPuntos = puntosActuales;

  function hexToRgba(hex, alpha) {
    const r = parseInt(hex.slice(1, 3), 16);
    const g = parseInt(hex.slice(3, 5), 16);
    const b = parseInt(hex.slice(5, 7), 16);
    return `rgba(${r}, ${g}, ${b}, ${alpha})`;
  }

  // Determina color de texto accesible (blanco/negro) según color de fondo.
  function getContrastColor(hex) {
    try {
      const r = parseInt(hex.slice(1, 3), 16);
      const g = parseInt(hex.slice(3, 5), 16);
      const b = parseInt(hex.slice(5, 7), 16);
      const luminance = (0.299 * r + 0.587 * g + 0.114 * b) / 255;
      return luminance > 0.6 ? "#000000" : "#ffffff";
    } catch {
      return "#ffffff";
    }
  }

  function renderTimeline() {
    const container = document.getElementById("timeline-container");
    if (!container) return;

    const levelData = calculateLevel(currentPuntos);
    if (!levelData) return;

    container.innerHTML = levels
      .map((nivel) => {
        const esActual = nivel.id === levelData.id;
        const esBloqueado = currentPuntos < nivel.puntosMin;
        const esCompletado = currentPuntos >= nivel.puntosMax;
        const progresoNivel = esActual ? levelData.progress : 0;

        let estadoClass = "";
        let estadoIcono = "";
        let estadoTexto = "";

        if (esActual) {
          estadoClass = "nivel-activo";
          estadoIcono = "🔥";
          estadoTexto = "En curso";
        } else if (esCompletado) {
          estadoClass = "";
          estadoIcono = "✅";
          estadoTexto = "Completado";
        } else {
          estadoClass = "nivel-bloqueado";
          estadoIcono = "🔒";
          estadoTexto = "Bloqueado";
        }

        return `
        <div class="nivel-timeline ${estadoClass} bg-[#e5e5e5] border-2 border-black/50 -xl p-4 relative" style="--nivel-color: ${nivel.color}; --nivel-color-glow: ${nivel.colorGlow}">
          <div class="flex items-center justify-between">
            <div class="flex items-center gap-3">
              <div class="w-12 h-12 -full flex items-center justify-center text-2xl" style="background: ${nivel.color}20;">
                ${nivel.icono}
              </div>
              <div>
                <h4 class="text-black font-bold">${nivel.nombre}</h4>
                <p class="text-gray-700 text-sm font-semibold">${nivel.rango}</p>
              </div>
            </div>
            <div class="flex items-center gap-2">
              <span class="text-xs font-medium px-2 py-1 -full" style="background: ${nivel.color}20; color: ${getContrastColor(nivel.color)};">
                ${estadoTexto}
              </span>
              ${esBloqueado ? '<svg class="w-4 h-4 text-black font-semibold" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M5 9V7a5 5 0 0110 0v2a2 2 0 012 2v5a2 2 0 01-2 2H5a2 2 0 01-2-2v-5a2 2 0 012-2zm8-2v2H7V7a3 3 0 016 0z" clip-rule="evenodd"/></svg>' : ""}
            </div>
          </div>
          ${
            esActual
              ? `
            <div class="mt-3">
              <div class="flex items-center gap-3">
                <div class="flex-1">
                  <div class="h-2 bg-slate-700 -full overflow-hidden" role="progressbar" aria-valuemin="0" aria-valuemax="100" aria-valuenow="${Math.round(progresoNivel)}" aria-label="Progreso del nivel ${nivel.nombre}">
                    <div class="h-full -full transition-all duration-500" style="width: ${progresoNivel}%; background: ${nivel.color};"></div>
                  </div>
                </div>
                <div class="w-12 text-right text-xs font-bold text-black">${Math.round(progresoNivel)}%</div>
              </div>
            </div>
          `
              : ""
          }
        </div>
      `;
      })
      .join("");
  }

  function renderBeneficios() {
    const container = document.getElementById("beneficios-grid");
    if (!container) return;

    const levelData = calculateLevel(currentPuntos);
    if (!levelData) return;

    container.innerHTML = levels
      .map((nivel) => {
        const esBloqueado = currentPuntos < nivel.puntosMin;
        const esActual = nivel.id === levelData.id;

        return `
        <div class="beneficio-card ${esBloqueado ? "bloqueado opacity-50" : ""} bg-[#e5e5e5] border-2 border-black/30 border-4 border-black -xl p-4" style="--beneficio-color: ${nivel.color}">
          <div class="flex items-start gap-3">
            <div class="w-10 h-10  flex items-center justify-center text-xl shrink-0" style="background: ${nivel.color}20;">
              ${nivel.icono}
            </div>
            <div class="flex-1 min-w-0">
              <h4 class="text-black font-bold text-sm mb-1">${nivel.nombre}</h4>
              <ul class="text-gray-800 text-xs font-semibold space-y-1">
                ${nivel.beneficios.map((b) => `<li class="flex items-start gap-2"><span class="text-brand-lime mt-0.5">•</span>${b}</li>`).join("")}
              </ul>
            </div>
            ${esBloqueado ? '<svg class="w-5 h-5 text-black font-semibold shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/></svg>' : ""}
          </div>
        </div>
      `;
      })
      .join("");
  }

  function updateUI() {
    const levelData = calculateLevel(currentPuntos);
    if (!levelData) return; // XP insuficiente

    const { nombre, progress, nextXp, isMaxLevel, color, colorGlow } = levelData;

    const nivelBadge = document.getElementById("nivel-badge");
    const nivelText = document.querySelector(".nivel-text");
    const xpActual = document.getElementById("xp-actual");
    const xpSiguiente = document.getElementById("xp-siguiente");
    const xpFaltante = document.getElementById("xp-faltante");
    const progresoBar = document.getElementById("progreso-bar");
    const perfilCard = document.getElementById("perfil-card");
    const glowNivel = document.getElementById("glow-nivel");
    const xpIndicator = document.getElementById("xp-indicator");

    if (nivelBadge && nivelText) {
      nivelText.textContent = nombre;
      nivelBadge.style.borderColor = color;
      nivelBadge.style.color = color;
      nivelBadge.style.backgroundColor = `${color}20`;
    }

    if (xpActual) {
      xpActual.textContent = `${currentPuntos.toLocaleString()} XP`;
    }

    if (xpSiguiente) {
      xpSiguiente.textContent = isMaxLevel ? "MAX" : `${(currentPuntos + nextXp).toLocaleString()} XP`;
    }

    if (xpFaltante) {
      xpFaltante.textContent = isMaxLevel
        ? "¡Has alcanzado el nivel máximo!"
        : `Faltan ${nextXp.toLocaleString()} XP para el siguiente nivel`;
    }

    if (progresoBar) {
      progresoBar.style.width = `${levelData.progress}%`;
      progresoBar.style.background = `linear-gradient(90deg, ${color}, ${colorGlow})`;
      progresoBar.setAttribute("role", "progressbar");
      progresoBar.setAttribute("aria-valuemin", "0");
      progresoBar.setAttribute("aria-valuemax", "100");
      progresoBar.setAttribute(
        "aria-valuenow",
        Math.round(levelData.progress).toString(),
      );
      progresoBar.setAttribute(
        "aria-label",
        `Progreso ${Math.round(levelData.progress)} por ciento`,
      );
    }

    if (perfilCard) {
      perfilCard.style.borderColor = color;
      perfilCard.style.boxShadow = `0 0 20px ${hexToRgba(color, 0.15)}`;
    }

    if (glowNivel) {
      glowNivel.style.background = hexToRgba(color, 0.2);
    }

    if (xpIndicator) {
      xpIndicator.style.backgroundColor = "#D7FD49";
      xpIndicator.style.boxShadow = `0 0 8px rgba(215,253,73,0.6)`;
    }

    // Actualizar bordes de otras secciones
    const timelineSection = document.getElementById("timeline-section");
    const beneficiosSection = document.getElementById("beneficios-section");

    if (timelineSection) {
      timelineSection.style.borderColor = color;
      timelineSection.style.boxShadow = `0 0 20px ${hexToRgba(color, 0.1)}`;
    }

    if (beneficiosSection) {
      beneficiosSection.style.borderColor = color;
      beneficiosSection.style.boxShadow = `0 0 20px ${hexToRgba(color, 0.1)}`;
    }

    // Ajustar badge a contraste mínimo para evitar texto ilegible en niveles claros.
    if (nivelBadge) {
      const textColor = getContrastColor(color);
      nivelBadge.style.borderColor = color;
      nivelBadge.style.color = textColor;
      nivelBadge.style.backgroundColor = `${color}20`;
      nivelBadge.style.boxShadow = `0 0 15px ${hexToRgba(color, 0.25)}`;
    }

    // Reflejar progreso numérico junto a la barra principal.
    const xpPorcentajeEl = document.getElementById("xp-porcentaje");
    if (xpPorcentajeEl) {
      xpPorcentajeEl.textContent = `${Math.round(levelData.progress)}%`;
    }

    renderTimeline();
    renderBeneficios();
  }

  function actualizarXP(nuevoXP) {
    currentPuntos = nuevoXP;
    updateUI();
  }

  function initModuloNiveles() {
    updateUI();
    window.actualizarXPModulo = actualizarXP;
    window.showXPInfo = showXPInfoModal;
    window.closeXPInfo = closeXPInfoModal;

    // Obtener nombre del usuario del localStorage
    try {
      const userStr = localStorage.getItem("capypay_user");
      if (userStr) {
        const user = JSON.parse(userStr);
        const userName = user.nombre || user.name;
        if (userName) {
          const firstName = userName.split(" ")[0];
          const userGreeting = document.getElementById("user-greeting");
          if (userGreeting) {
            userGreeting.textContent = firstName;
          }
        }
      }
    } catch (e) {
      console.error("Error al obtener nombre de usuario:", e);
    }
  }

  function showXPInfoModal() {
    const modal = document.getElementById("xp-info-modal");
    if (!modal) return;

    modal.classList.remove("hidden");
    modal.classList.add("flex");

    requestAnimationFrame(() => {
      modal.classList.remove("opacity-0");
      modal.querySelector("div").classList.remove("scale-95");
      modal.querySelector("div").classList.add("scale-100");
    });
  }

  function closeXPInfoModal() {
    const modal = document.getElementById("xp-info-modal");
    if (!modal) return;

    modal.classList.add("opacity-0");
    modal.querySelector("div").classList.add("scale-95");
    modal.querySelector("div").classList.remove("scale-100");

    setTimeout(() => {
      modal.classList.add("hidden");
      modal.classList.remove("flex");
    }, 300);
  }

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", () => {
      initModuloNiveles();

      const modal = document.getElementById("xp-info-modal");
      if (modal) {
        modal.addEventListener("click", (e) => {
          if (e.target === modal) {
            closeXPInfoModal();
          }
        });
      }
    });
  } else {
    initModuloNiveles();

    const modal = document.getElementById("xp-info-modal");
    if (modal) {
      modal.addEventListener("click", (e) => {
        if (e.target === modal) {
          closeXPInfoModal();
        }
      });
    }
  }
</script>
