---
const { class: className = "", as: Element = "div", ...rest } = Astro.props;
---

<Element
  class={`bg-white border-4 border-black shadow-[6px_6px_0px_0px_rgba(0,0,0,1)] p-6 ${className}`}
  {...rest}
>
  <slot />
</Element>

