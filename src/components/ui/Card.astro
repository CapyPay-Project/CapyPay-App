---
const { class: className = "", as: Element = "div", ...rest } = Astro.props;
---

<Element
  class={`bg-brand-black/80 backdrop-blur-xl border border-white/10 rounded-3xl p-6 shadow-lg ${className}`}
  {...rest}
>
  <slot />
</Element>
